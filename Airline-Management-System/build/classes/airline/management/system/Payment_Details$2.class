package airline.management.system;

import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;
import java.sql.ResultSet;
import java.sql.SQLException;
import net.proteanit.sql.DbUtils;

class Payment_Details$2 implements ActionListener {
    Payment_Details$2(Payment_Details this$0) {
        this.this$0 = this$0;
    }

    public void actionPerformed(ActionEvent ae) {
        try {
            String code = Payment_Details.access$100(this.this$0).getText();
            this.this$0.pst = Payment_Details.mycon.prepareStatement("select pnr_no,paid_amt,pay_date,cheque_no,card_no,ph_no from airline.payment where pnr_no=?");
            this.this$0.pst.setString(1, code);
            ResultSet myrs = this.this$0.pst.executeQuery();
            Payment_Details.access$200(this.this$0).setModel(DbUtils.resultSetToTableModel(myrs));
        } catch (SQLException var4) {
            var4.printStackTrace();
        }

    }
}

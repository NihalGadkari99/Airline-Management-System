package airline.management.system;

import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;
import java.sql.ResultSet;
import java.sql.SQLException;
import net.proteanit.sql.DbUtils;

class Payment_Details$1 implements ActionListener {
    Payment_Details$1(Payment_Details this$0) {
        this.this$0 = this$0;
    }

    public void actionPerformed(ActionEvent ae) {
        try {
            String code = this.this$0.textField.getText();
            conn c = new conn();
            String str = "select pnr_no,paid_amt,pay_date,cheque_no,card_no,ph_no from payment where pnr_no = '" + code + "'";
            ResultSet rs = c.s.executeQuery(str);
            this.this$0.table.setModel(DbUtils.resultSetToTableModel(rs));
        } catch (SQLException var6) {
            var6.printStackTrace();
        }

    }
}
